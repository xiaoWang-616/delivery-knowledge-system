# 项目画像模板

> 用途：接入一个真实项目时，先填写本模板。AI 开发任何模块前，都必须先阅读项目画像。

## 1. 项目基础信息

| 项目项 | 内容 |
| --- | --- |
| 项目名称 |  |
| 项目路径 |  |
| 当前分支 |  |
| 项目类型 | 前端 / 后端 / 全栈 / 移动端 / 其他 |
| 主要技术栈 |  |
| 包管理器 | npm / pnpm / yarn / bun / 其他 |
| 启动命令 |  |
| 类型检查命令 |  |
| lint 命令 |  |
| build 命令 |  |
| 测试命令 |  |

## 2. 项目目标

说明这个项目解决什么问题，面向什么用户，核心业务模块有哪些。

```text

```

## 3. 技术栈画像

| 分类 | 当前项目使用 | 备注 |
| --- | --- | --- |
| 构建工具 |  |  |
| 前端框架 |  |  |
| 路由 |  |  |
| 数据请求 |  |  |
| 服务端状态 |  |  |
| 客户端状态 |  |  |
| 样式方案 |  |  |
| 组件库 |  |  |
| 表单方案 |  |  |
| 测试方案 |  |  |
| 权限方案 |  |  |

## 4. 目录结构画像

记录项目主要目录职责，不需要穷举每个文件。

```text
src/
  pages/        # 
  features/     # 
  components/   # 
  lib/          # 
  styles/       # 
```

## 5. 路由规则

说明路由在哪里定义、如何新增页面、是否支持浏览器返回、是否有权限守卫。

```text

```

## 6. 接口调用规则

说明项目如何请求接口，页面是否允许直接请求，接口文件应该放在哪里。

```text

```

必须明确：

- 页面是否允许直接 `fetch`。
- 是否统一使用 `httpClient`。
- API 文件放在哪里。
- 类型文件放在哪里。
- query key 如何命名。
- 写操作成功后如何刷新数据。

## 7. 样式规则

说明样式参考源、主题色、组件密度、布局规范。

```text

```

必须明确样式优先级，例如：

```text
设计图 / Demo > 当前项目组件规范 > 旧项目样式 > AI 自行发挥
```

## 8. 组件复用规则

说明哪些组件必须优先复用，什么时候可以新增组件。

```text

```

## 9. 权限和登录规则

说明登录方式、token 存储、路由守卫、按钮权限、角色权限。

```text

```

## 10. 性能规则

说明列表、搜索、分页、缓存、局部刷新等要求。

```text

```

建议至少明确：

- 列表是否必须服务端分页。
- 搜索是否需要 debounce。
- 是否禁止整页 `reload`。
- 写操作后是否只刷新相关 query。
- 是否禁止前端一次拉大量数据再过滤。

## 11. 已知风险

| 风险 | 影响 | 处理方式 |
| --- | --- | --- |
|  |  |  |

## 12. AI 开发前必须阅读

列出 AI 开发前必须阅读的文档路径。

```text

```

## 13. 项目接入结论

说明这个项目适合怎样的开发方式。

```text
复杂度：S / M / L / XL
建议交付方式：
是否允许一次性生成代码：
是否需要分步骤验收：
```

