# 项目规则提取模板

> 用途：从已有项目中提取“必须延续”的规则，避免 AI 写出和原项目风格、架构不一致的代码。

## 1. 架构规则

```text
页面放在哪里：
业务模块放在哪里：
接口放在哪里：
类型放在哪里：
hooks 放在哪里：
通用组件放在哪里：
工具函数放在哪里：
```

## 2. 命名规则

```text
页面命名：
组件命名：
hooks 命名：
API 函数命名：
类型命名：
query key 命名：
CSS class 命名：
```

## 3. 接口规则

```text
是否统一 httpClient：
页面是否禁止直接 fetch：
是否统一 query hook：
错误处理方式：
token 注入方式：
接口路径是否允许硬编码：
```

## 4. 数据刷新规则

```text
列表请求：
详情请求：
新增成功后：
编辑成功后：
删除成功后：
状态变更成功后：
是否允许 window.location.reload：
```

## 5. 表单规则

```text
表单组件：
校验方式：
错误提示：
提交 loading：
取消行为：
提交成功行为：
```

## 6. 列表规则

```text
分页方式：
搜索触发：
搜索 debounce：
筛选状态：
重置行为：
空态：
加载态：
错误态：
```

## 7. 样式规则

```text
主题色：
页面背景：
卡片圆角：
按钮高度：
输入框高度：
表格行高：
字体大小：
图标来源：
弹窗宽度：
```

## 8. 交互规则

```text
页面跳转：
浏览器返回：
弹窗关闭：
确认危险操作：
tooltip：
状态标签：
```

## 9. 禁止事项

列出 AI 绝对不能做的事。

```text
- 不允许：
- 不允许：
- 不允许：
```

## 10. 可复用经验

记录当前项目已经验证过的好模式。

```text
- 
- 
- 
```

## 11. 项目规则结论

用 10 条以内总结这个项目最重要的规则。

```text
1.
2.
3.
```

